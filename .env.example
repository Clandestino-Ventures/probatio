# ──────────────────────────────────────────────────────────────────────────────
# PROBATIO — Environment Variables
# ──────────────────────────────────────────────────────────────────────────────
# Copy this file to `.env.local` and fill in the values.
# NEVER commit `.env.local` or any file containing real secrets.
# ──────────────────────────────────────────────────────────────────────────────

# ── Supabase ──────────────────────────────────────────────────────────────────
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# ── Stripe ────────────────────────────────────────────────────────────────────
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Subscription plan price IDs (create via Stripe Dashboard or setup-stripe script)
STRIPE_PRICE_STARTER_MONTHLY=price_...
STRIPE_PRICE_STARTER_ANNUAL=price_...
STRIPE_PRICE_PROFESSIONAL_MONTHLY=price_...
STRIPE_PRICE_PROFESSIONAL_ANNUAL=price_...
STRIPE_PRICE_ENTERPRISE_MONTHLY=price_...
STRIPE_PRICE_ENTERPRISE_ANNUAL=price_...

# Forensic product price IDs (one-time)
STRIPE_PRICE_FORENSIC_STANDARD=price_...
STRIPE_PRICE_FORENSIC_EXPERT=price_...

# ── Inngest (Pipeline Orchestration) ─────────────────────────────────────────
INNGEST_EVENT_KEY=your-inngest-event-key
INNGEST_SIGNING_KEY=your-inngest-signing-key

# ── Modal (GPU Compute) ──────────────────────────────────────────────────────
MODAL_TOKEN_ID=your-modal-token-id
MODAL_TOKEN_SECRET=your-modal-token-secret
# Alternative: single combined auth token (takes precedence over ID+SECRET)
MODAL_AUTH_TOKEN=
#
# URL Pattern: MODAL_BASE_URL + endpoint path from src/lib/modal/endpoints.ts
# The client constructs: ${MODAL_BASE_URL}/api/normalize, /api/separate, etc.
#
# To get MODAL_BASE_URL:
#   1. cd modal && bash deploy.sh
#   2. Modal prints URLs like: https://yourworkspace--spectra-forensic-audio-normalize-endpoint.modal.run
#   3. The base URL is the part before the function-specific path
#   4. Set MODAL_BASE_URL to the shared prefix
#
# Endpoint paths (from src/lib/modal/endpoints.ts):
#   /api/normalize          — Audio normalization (EBU R128)
#   /api/fingerprint        — Chromaprint + AcoustID
#   /api/separate           — Demucs source separation
#   /api/extract-features   — CREPE + librosa feature extraction
#   /api/generate-embeddings — CLAP multi-dimensional embeddings
#   /api/extract-lyrics     — Whisper large-v3 transcription
#   /api/search-matches     — pgvector similarity search
#   /api/enrich-rights      — MusicBrainz rights lookup
#   /api/dtw-compare        — Dynamic Time Warping comparison
#
# For local development without GPU, set MODAL_MOCK=true (uses mock data)
MODAL_MOCK=true
MODAL_BASE_URL=
MODAL_ENDPOINT_URL=

# ── Anthropic (AI Analysis) ──────────────────────────────────────────────────
ANTHROPIC_API_KEY=sk-ant-...

# ── AcoustID (Audio Fingerprinting) ──────────────────────────────────────────
ACOUSTID_API_KEY=your-acoustid-api-key

# ── Application ──────────────────────────────────────────────────────────────
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ── Monitoring & Analytics ───────────────────────────────────────────────────
# Sentry — https://sentry.io → Project Settings → Client Keys (DSN)
NEXT_PUBLIC_SENTRY_DSN=https://...@sentry.io/...
SENTRY_AUTH_TOKEN=sntrys_...
SENTRY_ORG=clandestino-ventures
SENTRY_PROJECT=spectra
# PostHog — https://app.posthog.com → Project Settings
NEXT_PUBLIC_POSTHOG_KEY=phc_...
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com

# ── Ed25519 Document Signing ─────────────────────────────────────────────────
# Generate with: npx tsx scripts/generate-signing-key.ts
PROBATIO_SIGNING_PRIVATE_KEY=your-private-key-base64
PROBATIO_SIGNING_PUBLIC_KEY=your-public-key-base64

# ── Demo Mode ────────────────────────────────────────────────────────────────
# Set to "true" to populate the dashboard with realistic mock data (no backend required).
NEXT_PUBLIC_DEMO_MODE=true
